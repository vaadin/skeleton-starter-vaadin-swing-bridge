<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- Project from https://start.vaadin.com/454493bd-4c1f-414d-bed8-1c1d7a48f4be -->
    <groupId>com.example</groupId>
    <artifactId>skeleton-starter-vaadin-swing-bridge</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    
    <properties>
        <!-- The Spring Boot parent derives maven.compiler.release from
             java.version; setting maven.compiler.source alone has no effect. -->
        <java.version>21</java.version>
        <vaadin.version>25.2.2</vaadin.version>
        <swing-bridge.version>1.3.0</swing-bridge.version>
        <swing-bridge.path>${settings.localRepository}/com/vaadin</swing-bridge.path>
    </properties>
    
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-bom</artifactId>
                <version>${vaadin.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
    
    <dependencies>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>swing-bridge-patch</artifactId>
            <version>${swing-bridge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>swing-bridge-graphics</artifactId>
            <version>${swing-bridge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>swing-bridge-flow</artifactId>
            <version>${swing-bridge.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.logging.log4j</groupId>
                    <artifactId>log4j-slf4j2-impl</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>com.vaadin</groupId>
            <!-- Replace artifactId with vaadin-core to use only free components -->
            <artifactId>vaadin</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.vaadin</groupId>
            <artifactId>vaadin-dev</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>spring-boot:run</defaultGoal>
        <plugins>
            <plugin>
                <groupId>com.vaadin</groupId>
                <artifactId>vaadin-maven-plugin</artifactId>
                <version>${vaadin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-frontend</goal>
                            <goal>build-frontend</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <!-- All JVM flags as one space-separated string. Do not put
                         XML comments inside the element: its content is passed
                         to the JVM verbatim.

                         The last line is the debug agent for `mvn
                         spring-boot:run` — attach an IDE debugger to
                         localhost:5005. It is bound to loopback on purpose;
                         `address=*:5005` would accept connections from the
                         network. -->
                    <jvmArguments>
                        --patch-module java.desktop=${swing-bridge.path}/swing-bridge-patch/${swing-bridge.version}/swing-bridge-patch-${swing-bridge.version}.jar
                        -Xbootclasspath/a:${swing-bridge.path}/swing-bridge-graphics/${swing-bridge.version}/swing-bridge-graphics-${swing-bridge.version}.jar
                        --add-reads=java.desktop=ALL-UNNAMED
                        --add-exports=java.desktop/sun.font=ALL-UNNAMED
                        --add-exports=java.desktop/sun.awt=ALL-UNNAMED
                        --add-exports=java.desktop/sun.awt.dnd=ALL-UNNAMED
                        --add-exports=java.desktop/sun.awt.dnd.peer=ALL-UNNAMED
                        --add-exports=java.base/sun.nio.cs=ALL-UNNAMED
                        --add-exports=java.desktop/sun.java2d=ALL-UNNAMED
                        --add-exports=java.desktop/sun.java2d.pipe=ALL-UNNAMED
                        --add-exports=java.desktop/sun.awt.datatransfer=ALL-UNNAMED
                        --add-exports=java.desktop/sun.awt.image=ALL-UNNAMED
                        --add-exports=java.desktop/java.awt.peer=ALL-UNNAMED
                        --add-exports=java.desktop/java.awt.dnd=ALL-UNNAMED
                        --add-exports=java.desktop/java.awt.dnd.peer=ALL-UNNAMED
                        --add-exports=java.desktop/sun.print=ALL-UNNAMED
                        --add-exports=java.desktop/sun.swing=ALL-UNNAMED
                        --add-opens=java.desktop/java.awt.event=ALL-UNNAMED
                        --add-opens=java.desktop/sun.awt=ALL-UNNAMED
                        --add-opens=java.desktop/java.awt.dnd=ALL-UNNAMED
                        --add-opens=java.base/java.lang=ALL-UNNAMED
                        -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=localhost:5005
                    </jvmArguments>

                    <!-- JVM -D system properties -->
                    <systemPropertyVariables>
                        <!-- Required: SwingBridge needs a non-headless AWT toolkit. -->
                        <java.awt.headless>false</java.awt.headless>

                        <!-- Directory scanned for the Swing application's JARs.
                             Defaults to applibs/ next to the project. -->
                        <!--<applibs.dir>${project.basedir}/some-lib-dir</applibs.dir>-->

                        <!-- SwingBridge tuning. All optional; the defaults below
                             are what you get if you leave these commented out.
                             See the Configuration and Display and Rendering pages
                             of the SwingBridge documentation. -->

                        <!-- Screen size reported to the Swing application:
                             per-user (default) or a fixed <width>x<height>. -->
                        <!--<swingbridge.screenSize>per-user</swingbridge.screenSize>-->

                        <!-- Render at the browser's device pixel ratio for crisp
                             text on HiDPI displays: off (default), auto, or a
                             decimal such as 2. Costs heap per window. -->
                        <!--<swingbridge.hidpi>auto</swingbridge.hidpi>-->

                        <!-- Back-buffer pixel format: adaptive (default), argb,
                             or 565 to halve back-buffer memory at the cost of
                             color fidelity. -->
                        <!--<swingbridge.backBufferType>adaptive</swingbridge.backBufferType>-->

                        <!-- The canvas takes browser focus when it appears, so
                             typing works without clicking it first. Default true. -->
                        <!--<swingbridge.autoFocusOnAttach>true</swingbridge.autoFocusOnAttach>-->

                        <!-- Prefix each Swing application's console output with
                             its run ID, to tell concurrent users apart. -->
                        <!--<swingbridge.consoleLogPrefix>true</swingbridge.consoleLogPrefix>-->

                        <!-- Hide the exception details the launch-failure view
                             shows when the Swing application cannot start. -->
                        <!--<swingbridge.errorReporting.enabled>false</swingbridge.errorReporting.enabled>-->
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
